# Persistent application state remains on the NVMe root filesystem.
# K3s local-path-provisioner creates one subdirectory per persistent volume.
d /srv/operational 0750 root root -
d /srv/operational/k3s 0750 root root -
d /srv/operational/k3s/local-path 0750 root root -
