services:
  filebrowser:
    image: filebrowser/filebrowser:latest
    container_name: filebrowser
    restart: unless-stopped
    user: "${PUID:-1000}:${PGID:-1000}"
    healthcheck:
      disable: true
    command:
      - --address
      - 0.0.0.0
      - --port
      - "8080"
      - --root
      - /srv/shared
      - --database
      - /database/filebrowser.db
    volumes:
      - "${SHARED_ROOT:-/srv/shared}:/srv/shared"
      - "${CONFIG_ROOT:-/var/lib/penzance/config}/filebrowser:/database"
      - "${CONFIG_ROOT:-/var/lib/penzance/config}/filebrowser:/config"
    networks:
      - edge
