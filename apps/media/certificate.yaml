apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: media-home-rpca-uk
  namespace: media
spec:
  secretName: media-home-rpca-uk-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-cloudflare
  dnsNames:
    - jellyfin.home.rpca.uk
    - navidrome.home.rpca.uk
    - navidrom.home.rpca.uk
